package com.addiPlot.gnuplot;

import java.io.File;

public class GlobalMembersAnsi2knr
{
	///#define __STDC__ 1
	///#define __STDC_HOSTED__ 1
	///#define __GNUC__ 4
	///#define __GNUC_MINOR__ 3
	///#define __GNUC_PATCHLEVEL__ 6
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __SIZE_TYPE__ long unsigned int
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __PTRDIFF_TYPE__ long int
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __WCHAR_TYPE__ int
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __WINT_TYPE__ unsigned int
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __INTMAX_TYPE__ long int
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __UINTMAX_TYPE__ long unsigned int
	///#define __GXX_ABI_VERSION 1002
	///#define __SCHAR_MAX__ 127
	///#define __SHRT_MAX__ 32767
	///#define __INT_MAX__ 2147483647
	///#define __LONG_MAX__ 9223372036854775807L
	///#define __LONG_LONG_MAX__ 9223372036854775807LL
	///#define __WCHAR_MAX__ 2147483647
	///#define __CHAR_BIT__ 8
	///#define __INTMAX_MAX__ 9223372036854775807L
	///#define __FLT_EVAL_METHOD__ 0
	///#define __DEC_EVAL_METHOD__ 2
	///#define __FLT_RADIX__ 2
	///#define __FLT_MANT_DIG__ 24
	///#define __FLT_DIG__ 6
	///#define __FLT_MIN_EXP__ (-125)
	///#define __FLT_MIN_10_EXP__ (-37)
	///#define __FLT_MAX_EXP__ 128
	///#define __FLT_MAX_10_EXP__ 38
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __FLT_MAX__ 3.40282347e+38F
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __FLT_MIN__ 1.17549435e-38F
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __FLT_EPSILON__ 1.19209290e-7F
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __FLT_DENORM_MIN__ 1.40129846e-45F
	///#define __FLT_HAS_DENORM__ 1
	///#define __FLT_HAS_INFINITY__ 1
	///#define __FLT_HAS_QUIET_NAN__ 1
	///#define __DBL_MANT_DIG__ 53
	///#define __DBL_DIG__ 15
	///#define __DBL_MIN_EXP__ (-1021)
	///#define __DBL_MIN_10_EXP__ (-307)
	///#define __DBL_MAX_EXP__ 1024
	///#define __DBL_MAX_10_EXP__ 308
	///#define __DBL_MAX__ 1.7976931348623157e+308
	///#define __DBL_MIN__ 2.2250738585072014e-308
	///#define __DBL_EPSILON__ 2.2204460492503131e-16
	///#define __DBL_DENORM_MIN__ 4.9406564584124654e-324
	///#define __DBL_HAS_DENORM__ 1
	///#define __DBL_HAS_INFINITY__ 1
	///#define __DBL_HAS_QUIET_NAN__ 1
	///#define __LDBL_MANT_DIG__ 64
	///#define __LDBL_DIG__ 18
	///#define __LDBL_MIN_EXP__ (-16381)
	///#define __LDBL_MIN_10_EXP__ (-4931)
	///#define __LDBL_MAX_EXP__ 16384
	///#define __LDBL_MAX_10_EXP__ 4932
	///#define __DECIMAL_DIG__ 21
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __LDBL_MAX__ 1.18973149535723176502e+4932L
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __LDBL_MIN__ 3.36210314311209350626e-4932L
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __LDBL_EPSILON__ 1.08420217248550443401e-19L
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __LDBL_DENORM_MIN__ 3.64519953188247460253e-4951L
	///#define __LDBL_HAS_DENORM__ 1
	///#define __LDBL_HAS_INFINITY__ 1
	///#define __LDBL_HAS_QUIET_NAN__ 1
	///#define __DEC32_MANT_DIG__ 7
	///#define __DEC32_MIN_EXP__ (-95)
	///#define __DEC32_MAX_EXP__ 96
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC32_MIN__ 1E-95DF
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC32_MAX__ 9.999999E96DF
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC32_EPSILON__ 1E-6DF
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC32_DEN__ 0.000001E-95DF
	///#define __DEC64_MANT_DIG__ 16
	///#define __DEC64_MIN_EXP__ (-383)
	///#define __DEC64_MAX_EXP__ 384
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC64_MIN__ 1E-383DD
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC64_MAX__ 9.999999999999999E384DD
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC64_EPSILON__ 1E-15DD
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC64_DEN__ 0.000000000000001E-383DD
	///#define __DEC128_MANT_DIG__ 34
	///#define __DEC128_MIN_EXP__ (-6143)
	///#define __DEC128_MAX_EXP__ 6144
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC128_MIN__ 1E-6143DL
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC128_MAX__ 9.999999999999999999999999999999999E6144DL
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC128_EPSILON__ 1E-33DL
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define __DEC128_DEN__ 0.000000000000000000000000000000001E-6143DL
	///#define __REGISTER_PREFIX__
	///#define __USER_LABEL_PREFIX__
	///#define __VERSION__ "4.3.6"
	///#define __GNUC_GNU_INLINE__ 1
	///#define _LP64 1
	///#define __LP64__ 1
	///#define __OPTIMIZE__ 1
	///#define __FINITE_MATH_ONLY__ 0
	///#define __GCC_HAVE_SYNC_COMPARE_AND_SWAP_1 1
	///#define __GCC_HAVE_SYNC_COMPARE_AND_SWAP_2 1
	///#define __GCC_HAVE_SYNC_COMPARE_AND_SWAP_4 1
	///#define __GCC_HAVE_SYNC_COMPARE_AND_SWAP_8 1
	///#define __SIZEOF_INT__ 4
	///#define __SIZEOF_LONG__ 8
	///#define __SIZEOF_LONG_LONG__ 8
	///#define __SIZEOF_SHORT__ 2
	///#define __SIZEOF_FLOAT__ 4
	///#define __SIZEOF_DOUBLE__ 8
	///#define __SIZEOF_LONG_DOUBLE__ 16
	///#define __SIZEOF_SIZE_T__ 8
	///#define __SIZEOF_WCHAR_T__ 4
	///#define __SIZEOF_WINT_T__ 4
	///#define __SIZEOF_PTRDIFF_T__ 8
	///#define __SIZEOF_POINTER__ 8
	///#define __amd64 1
	///#define __amd64__ 1
	///#define __x86_64 1
	///#define __x86_64__ 1
	///#define __k8 1
	///#define __k8__ 1
	///#define __MMX__ 1
	///#define __SSE__ 1
	///#define __SSE2__ 1
	///#define __SSE_MATH__ 1
	///#define __SSE2_MATH__ 1
	///#define __gnu_linux__ 1
	///#define __linux 1
	///#define __linux__ 1
	///#define linux 1
	///#define __unix 1
	///#define __unix__ 1
	///#define unix 1
	///#define __ELF__ 1
	///#define __DECIMAL_BID_FORMAT__ 1
	///#define HAVE_CONFIG_H 1
	///#define BINDIR "/usr/local/bin"
	///#define X11_DRIVER_DIR "/usr/local/libexec/gnuplot/4.4"
	///#define GNUPLOT_PS_DIR "/usr/local/share/gnuplot/4.4/PostScript"
	///#define GNUPLOT_JS_DIR "/usr/local/share/gnuplot/4.4/js"
	///#define GNUPLOT_LUA_DIR "/usr/local/share/gnuplot/4.4/lua"
	///#define CONTACT "gnuplot-bugs@lists.sourceforge.net"
	///#define HELPFILE "/usr/local/share/gnuplot/4.4/gnuplot.gih"
	///#define GNUPLOT_X11 "gnuplot_x11"
	///#define XAPPLRESDIR "/etc/X11/app-defaults/"

	/* Copyright (C) 1989, 2000 Aladdin Enterprises.  All rights reserved. */

	/*$Id: ansi2knr.c,v 1.3 2002/12/07 18:44:15 lhecking Exp $*/
	/* Convert ANSI C function definitions to K&R ("traditional C") syntax */

	/*
	ansi2knr is distributed in the hope that it will be useful, but WITHOUT ANY
	WARRANTY.  No author or distributor accepts responsibility to anyone for the
	consequences of using it or for whether it serves any particular purpose or
	works at all, unless he says so in writing.  Refer to the GNU General Public
	License (the "GPL") for full details.
	
	Everyone is granted permission to copy, modify and redistribute ansi2knr,
	but only under the conditions described in the GPL.  A copy of this license
	is supposed to have been given to you along with ansi2knr so you can know
	your rights and responsibilities.  It should be in a file named COPYLEFT,
	or, if there is no file named COPYLEFT, a file named COPYING.  Among other
	things, the copyright notice and this notice must be preserved on all
	copies.
	
	We explicitly state here what we believe is already implied by the GPL: if
	the ansi2knr program is distributed as a separate set of sources and a
	separate executable file which are aggregated on a storage medium together
	with another program, this in itself does not bring the other program under
	the GPL, nor does the mere fact that such a program or the procedures for
	constructing it invoke the ansi2knr executable bring any other part of the
	program under the GPL.
	*/

	/*
	 * Usage:
		ansi2knr [--filename FILENAME] [INPUT_FILE [OUTPUT_FILE]]
	 * --filename provides the file name for the #line directive in the output,
	 * overriding input_file (if present).
	 * If no input_file is supplied, input is read from stdin.
	 * If no output_file is supplied, output goes to stdout.
	 * There are no error messages.
	 *
	 * ansi2knr recognizes function definitions by seeing a non-keyword
	 * identifier at the left margin, followed by a left parenthesis, with a
	 * right parenthesis as the last character on the line, and with a left
	 * brace as the first token on the following line (ignoring possible
	 * intervening comments and/or preprocessor directives), except that a line
	 * consisting of only
	 *	identifier1(identifier2)
	 * will not be considered a function definition unless identifier2 is
	 * the word "void", and a line consisting of
	 *	identifier1(identifier2, <<arbitrary>>)
	 * will not be considered a function definition.
	 * ansi2knr will recognize a multi-line header provided that no intervening
	 * line ends with a left or right brace or a semicolon.  These algorithms
	 * ignore whitespace, comments, and preprocessor directives, except that
	 * the function name must be the first thing on the line.  The following
	 * constructs will confuse it:
	 *	- Any other construct that starts at the left margin and
	 *	    follows the above syntax (such as a macro or function call).
	 *	- Some macros that tinker with the syntax of function headers.
	 */

	/*
	 * The original and principal author of ansi2knr is L. Peter Deutsch
	 * <ghost@aladdin.com>.  Other authors are noted in the change history
	 * that follows (in reverse chronological order):
	
		lpd 2000-04-12 backs out Eggert's changes because of bugs:
		- concatlits didn't declare the type of its bufend argument;
		- concatlits didn't't recognize when it was inside a comment;
		- scanstring could scan backward past the beginning of the string; when
		- the check for \ + newline in scanstring was unnecessary.
	
		2000-03-05  Paul Eggert  <eggert@twinsun.com>
	
		Add support for concatenated string literals.
		* ansi2knr.c (concatlits): New decl.
		(main): Invoke concatlits to concatenate string literals.
		(scanstring): Handle backslash-newline correctly.  Work with
		character constants.  Fix bug when scanning backwards through
		backslash-quote.  Check for unterminated strings.
		(convert1): Parse character constants, too.
		(appendline, concatlits): New functions.
		* ansi2knr.1: Document this.
	
		lpd 1999-08-17 added code to allow preprocessor directives
			wherever comments are allowed
		lpd 1999-04-12 added minor fixes from Pavel Roskin
			<pavel_roskin@geocities.com> for clean compilation with
			gcc -W -Wall
		lpd 1999-03-22 added hack to recognize lines consisting of
			identifier1(identifier2, xxx) as *not* being procedures
		lpd 1999-02-03 made indentation of preprocessor commands consistent
		lpd 1999-01-28 fixed two bugs: a '/' in an argument list caused an
			endless loop; quoted strings within an argument list
			confused the parser
		lpd 1999-01-24 added a check for write errors on the output,
			suggested by Jim Meyering <meyering@ascend.com>
		lpd 1998-11-09 added further hack to recognize identifier(void)
			as being a procedure
		lpd 1998-10-23 added hack to recognize lines consisting of
			identifier1(identifier2) as *not* being procedures
		lpd 1997-12-08 made input_file optional; only closes input and/or
			output file if not stdin or stdout respectively; prints
			usage message on stderr rather than stdout; adds
			--filename switch (changes suggested by
			<ceder@lysator.liu.se>)
		lpd 1996-01-21 added code to cope with not HAVE_CONFIG_H and with
			compilers that don't understand void, as suggested by
			Tom Lane
		lpd 1996-01-15 changed to require that the first non-comment token
			on the line following a function header be a left brace,
			to reduce sensitivity to macros, as suggested by Tom Lane
			<tgl@sss.pgh.pa.us>
		lpd 1995-06-22 removed #ifndefs whose sole purpose was to define
			undefined preprocessor symbols as 0; changed all #ifdefs
			for configuration symbols to #ifs
		lpd 1995-04-05 changed copyright notice to make it clear that
			including ansi2knr in a program does not bring the entire
			program under the GPL
		lpd 1994-12-18 added conditionals for systems where ctype macros
			don't handle 8-bit characters properly, suggested by
			Francois Pinard <pinard@iro.umontreal.ca>;
			removed --varargs switch (this is now the default)
		lpd 1994-10-10 removed CONFIG_BROKETS conditional
		lpd 1994-07-16 added some conditionals to help GNU `configure',
			suggested by Francois Pinard <pinard@iro.umontreal.ca>;
			properly erase prototype args in function parameters,
			contributed by Jim Avera <jima@netcom.com>;
			correct error in writeblanks (it shouldn't erase EOLs)
		lpd 1989-xx-xx original version
	 */

	/* Most of the conditionals here are to make ansi2knr work with */
	/* or without the GNU configure machinery. */

	///#if HAVE_CONFIG_H
	///#endif


	///#if HAVE_CONFIG_H

	/*
	   For properly autoconfiguring ansi2knr, use AC_CONFIG_HEADER(config.h).
	   This will define HAVE_CONFIG_H and so, activate the following lines.
	 */

	///#if STDC_HEADERS || HAVE_STRING_H
	///#include <string.h>
	///#else
	///#endif

	///#else // not HAVE_CONFIG_H 
	//
	// /* Otherwise do it the hard way */
	//
	///#ifdef BSD
	///#include <strings.h>
	///#else
	///#ifdef VMS
	//    extern int strlen(), strncmp();
	///#else
	///#include <string.h>
	///#endif
	///#endif
	//
	///#endif // not HAVE_CONFIG_H 

	///#if STDC_HEADERS
	///#include <stdlib.h>
	///#else
	/*
	   malloc and free should be declared in stdlib.h,
	   but if you've got a K&R compiler, they probably aren't.
	 */
	///#ifdef MSDOS
	///#include <malloc.h>
	///#else
	///#ifdef VMS
	//     extern char *malloc();
	//     extern void free();
	///#else
//C++ TO JAVA CONVERTER TODO TASK: The implementation of the following method could not be found:
	//	 extern String malloc();
//C++ TO JAVA CONVERTER TODO TASK: The implementation of the following method could not be found:
	//	 extern int free();
	///#endif
	///#endif

	///#endif

	/* Define NULL (for *very* old compilers). */
	///#ifndef NULL
	///#define NULL (0)
	///#endif

	/*
	 * The ctype macros don't always handle 8-bit characters correctly.
	 * Compensate for this here.
	 */
	///#ifdef isascii
	///#undef HAVE_ISASCII		// just in case 
	///#define HAVE_ISASCII 1
	///#else
	///#endif
	///#if STDC_HEADERS || !HAVE_ISASCII
	///#define is_ascii(c) 1
	///#else
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define is_ascii(c) isascii(c)
	///#endif

	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define is_space(c) (isascii(c) && isspace(c))
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define is_alpha(c) (isascii(c) && isalpha(c))
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define is_alnum(c) (isascii(c) && isalnum(c))

	/* Scanning macros */
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define isidchar(ch) ((isascii(ch) && isalnum(ch)) || (ch) == '_')
	//C++ TO JAVA CONVERTER NOTE: The following #define macro was replaced in-line:
	///#define isidfirstchar(ch) ((isascii(ch) && isalpha(ch)) || (ch) == '_')

	/* Forward references */
//C++ TO JAVA CONVERTER TODO TASK: The implementation of the following method could not be found:
	//String ppdirforward();
//C++ TO JAVA CONVERTER TODO TASK: The implementation of the following method could not be found:
	//String ppdirbackward();
//C++ TO JAVA CONVERTER TODO TASK: The implementation of the following method could not be found:
	//String skipspace();
//C++ TO JAVA CONVERTER TODO TASK: The implementation of the following method could not be found:
	//String scanstring();
//C++ TO JAVA CONVERTER TODO TASK: The implementation of the following method could not be found:
	//int writeblanks();
//C++ TO JAVA CONVERTER TODO TASK: The implementation of the following method could not be found:
	//int test1();
//C++ TO JAVA CONVERTER TODO TASK: The implementation of the following method could not be found:
	//int convert1();

	/* The main program */
//C++ TO JAVA CONVERTER TODO TASK: The implementation of the following method could not be found:
	//static void Main(argc NamelessParameter1, args NamelessParameter2) int argc;
//C++ TO JAVA CONVERTER TODO TASK: Pointer arithmetic is detected on this variable, so pointers on this variable are left unchanged.
		public static String args;
		{
		public static File in = stdin;
		public static File out = stdout;
		public static String filename = 0;
		public static String program_name = args.charAt(0);
		public static String output_name = 0;
	///#define bufsize 5000
		public static String buf;
		public static String line;
		public static String more;
		public static String usage = "Usage: ansi2knr [--filename FILENAME] [INPUT_FILE [OUTPUT_FILE]]\n";
		/*
		 * In previous versions, ansi2knr recognized a --varargs switch.
		 * If this switch was supplied, ansi2knr would attempt to convert
		 * a ... argument to va_alist and va_dcl; if this switch was not
		 * supplied, ansi2knr would simply drop any such arguments.
		 * Now, ansi2knr always does this conversion, and we only
		 * check for this switch for backward compatibility.
		 */
		public static int convert_varargs = 1;
		public static int output_error;

//C++ TO JAVA CONVERTER TODO TASK: The following method format was not recognized, possibly due to an unrecognized macro:
		while (argc > 1 && args[1][0] == '-')
		{
		  if (!strcmp(args.charAt(1), "--varargs"))
		  {
			convert_varargs = 1;
			argc--;
			args++;
			continue;
		  }
		  if (!strcmp(args.charAt(1), "--filename") && argc > 2)
		  {
			filename = args.charAt(2);
			argc -= 2;
			args += 2;
			continue;
		  }
		  fprintf(stderr, "%s: Unrecognized switch: %s\n", program_name, args.charAt(1));
		  fprintf(stderr, usage);
		  System.exit(1);
		}
//C++ TO JAVA CONVERTER TODO TASK: The following method format was not recognized, possibly due to an unrecognized macro:
		switch (argc)
		{
		default:
			fprintf(stderr, usage);
			System.exit(0);
		case 3:
			output_name = args.charAt(2);
			out = fopen(output_name, "w");
			if (out == DefineConstants.NULL)
			{
			  fprintf(stderr, "%s: Cannot open output file %s\n", program_name, output_name);
			  System.exit(1);
			}
			/* falls through */
		case 2:
			in = fopen(args.charAt(1), "r");
			if (in == DefineConstants.NULL)
			{
			  fprintf(stderr, "%s: Cannot open input file %s\n", program_name, args.charAt(1));
			  System.exit(1);
			}
			if (filename.equals(0))
			  filename = args.charAt(1);
			/* falls through */
		case 1:
			break;
		}
//C++ TO JAVA CONVERTER TODO TASK: The following statement was not recognized, possibly due to an unrecognized macro:
		if (filename)
//C++ TO JAVA CONVERTER TODO TASK: The following statement was not recognized, possibly due to an unrecognized macro:
		  fprintf(out, "#line 1 \"%s\"\n", filename);
//C++ TO JAVA CONVERTER TODO TASK: The following statement was not recognized, possibly due to an unrecognized macro:
		buf = malloc(DefineConstants.bufsize);
//C++ TO JAVA CONVERTER TODO TASK: The following method format was not recognized, possibly due to an unrecognized macro:
		if (buf == DefineConstants.NULL)
		{
			fprintf(stderr, "Unable to allocate read buffer!\n");
			System.exit(1);
		}
//C++ TO JAVA CONVERTER TODO TASK: The following statement was not recognized, possibly due to an unrecognized macro:
		line = buf;
//C++ TO JAVA CONVERTER TODO TASK: The following method format was not recognized, possibly due to an unrecognized macro:
		while (fgets(line, (int)(buf + DefineConstants.bufsize - line), in) != DefineConstants.NULL)
		{
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
	test:
	line += line.length();
			switch (GlobalMembersAnsi2knr.test1(buf))
			{
			case 2: // a function header
				convert1(buf, out, 1, convert_varargs);
				break;
			case 1: // a function
				/* Check for a { at the start of the next line. */
				more = ++line;
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
	f: // overflow check
	if (line.compareTo(buf + (DefineConstants.bufsize - 1)) >= 0)
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
				  goto wl;
				if (fgets(line, (int)(buf + DefineConstants.bufsize - line), in) == DefineConstants.NULL)
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
				  goto wl;
				switch (skipspace(GlobalMembersAnsi2knr.ppdirforward(more), 1))
				{
				  case '{':
					/* Definitely a function header. */
					convert1(buf, out, 0, convert_varargs);
					fputs(more, out);
					break;
				  case 0:
					/* The next line was blank or a comment: */
					/* keep scanning for a non-comment. */
					line += line.length();
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
					goto f;
				  default:
					/* buf isn't a function header, but */
					/* more might be. */
					fputs(buf, out);
					buf = more;
					line = buf;
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
					goto test;
				}
				break;
			case -1: // maybe the start of a function
				if (!line.equals(buf + (DefineConstants.bufsize - 1))) // overflow check
				  continue;
				/* falls through */
			default: // not a function
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
	wl:
	fputs(buf, out);
				break;
			}
			line = buf;
		}
//C++ TO JAVA CONVERTER TODO TASK: The following statement was not recognized, possibly due to an unrecognized macro:
		if (line != buf)
//C++ TO JAVA CONVERTER TODO TASK: The following statement was not recognized, possibly due to an unrecognized macro:
		  fputs(buf, out);
//C++ TO JAVA CONVERTER TODO TASK: The following statement was not recognized, possibly due to an unrecognized macro:
		free(buf);
//C++ TO JAVA CONVERTER TODO TASK: The following method format was not recognized, possibly due to an unrecognized macro:
		if (output_name)
		{
		  output_error = ferror(out);
		  output_error |= fclose(out);
		} // out == stdout
		else
		{
		  fflush(out);
		  output_error = ferror(out);
		}
		if (output_error != 0)
		{
		  fprintf(stderr, "%s: error writing to %s\n", program_name, (output_name != null ? output_name : "stdout"));
		  System.exit(1);
		}
		if (in != stdin)
		  fclose(in);
		}

	/*
	 * Skip forward or backward over one or more preprocessor directives.
	 */
	String ppdirforward(p) byte p;
	{
		for (; p.equals('#'); ++p)
		{
		for (; !p.equals('\r') && !p.equals('\n'); ++p)
			if (p.equals(0))
			return p;
		if (p.equals('\r') && p.charAt(1) == '\n')
			++p;
		}
		return p;
	}
	String ppdirbackward(p, limit) byte p;
		String limit;
		{
		String np = p;

		for (;; p = --np)
		{
		if (np.equals('\n') && np.charAt(-1) == '\r')
			--np;
		for (; np.compareTo(limit) > 0 && np.charAt(-1) != '\r' && np.charAt(-1) != '\n'; --np)
			if (np.charAt(-1) == 0)
			return np;
		if (!np.equals('#'))
			return p;
		}
		}

	/*
	 * Skip over whitespace, comments, and preprocessor directives,
	 * in either direction.
	 */
	String skipspace(p, dir) byte p;
		int dir; // 1 for forward, -1 for backward
		{
		for (; ;)
		{
		while ((isascii(p) && Character.isWhitespace(p)))
			p += dir;
		if (!(p.equals('/') && p.charAt(dir) == '*'))
			break;
		p += dir;
		p += dir;
		while (!(p.equals('*') && p.charAt(dir) == '/'))
		{
			if (p.equals(0))
			return p; // multi-line comment??
			p += dir;
		}
		p += dir;
		p += dir;
		}
		return p;
		}

	/* Scan over a quoted string, in either direction. */
	String scanstring(p, dir) byte p;
		int dir;
		{
		for (p + = dir; ; p + = dir)
		if (p.equals('"') && p.charAt(-dir) != '\\')
			return p + dir;
		}

	/*
	 * Write blanks over part of a string.
	 * Don't overwrite end-of-line characters.
	 */
	int writeblanks(start, end) byte * start;
		String end;
		{
//C++ TO JAVA CONVERTER TODO TASK: Pointer arithmetic is detected on this variable, so pointers on this variable are left unchanged.
		byte * p;
		for (p = start; p < end; p++)
		  if (*p != '\r' && *p != '\n')
			*p = (byte)' ';
		return 0;
		}

	/*
	 * Test whether the string in buf is a function definition.
	 * The string may contain and/or end with a newline.
	 * Return as follows:
	 *	0 - definitely not a function definition;
	 *	1 - definitely a function definition;
	 *	2 - definitely a function prototype (NOT USED);
	 *	-1 - may be the beginning of a function definition,
	 *		append another line and look again.
	 * The reason we don't attempt to convert function prototypes is that
	 * Ghostscript's declaration-generating macros look too much like
	 * prototypes, and confuse the algorithms.
	 */
	int test1(buf) byte buf;
	{
		String p = buf;
		String bend;
		String endfn;
		int contin;

		if (!((isascii(p) && Character.isLetter(p)) || (p) == '_'))
		  return 0; // no name at left margin
		bend = skipspace(ppdirbackward(buf + buf.length() - 1, buf), -1);
		switch (bend)
		{
		   case ';': //2
			   contin = 0;
			   break;
		   case ')':
			   contin = 1;
			   break;
		   case '{': // not a function
			   return 0;
		   case '}': // not a function
			   return 0;
		   default:
			   contin = -1;
		}
		while (((isascii(p) && Character.isLetterOrDigit(p)) || (p) == '_'))
		  p++;
		endfn = p;
		p = skipspace(p, 1);
		if (p++ != '(')
		  return 0; // not a function
		p = skipspace(p, 1);
		if (p.equals(')'))
		  return 0; // no parameters
		/* Check that the apparent function name isn't a keyword. */
		/* We only need to check for keywords that could be followed */
		/* by a left parenthesis (which, unfortunately, is most of them). */
		  {
			   static[] byte * words = {"asm", "auto", "case", "char", "const", "double", "extern", "float", "for", "if", "int", "long", "register", "return", "short", "signed", "sizeof", "static", "switch", "typedef", "unsigned", "void", "volatile", "while", 0};
//C++ TO JAVA CONVERTER TODO TASK: Pointer arithmetic is detected on this variable, so pointers on this variable are left unchanged.
			String key = words;
			String kp;
			int len = endfn - buf;

			while ((kp = key) != 0)
			{
				   if (kp.length() == len && !strncmp(kp, buf, len))
				  return 0; // name is a keyword
				key++;
			}
		  }
		  {
			   String id = p;
			   int len;
			   /*
			* Check for identifier1(identifier2) and not
			* identifier1(void), or identifier1(identifier2, xxxx).
			*/

			   while (((isascii(p) && Character.isLetterOrDigit(p)) || (p) == '_'))
			   p++;
			   len = p - id;
			   p = skipspace(p, 1);
			   if (p.equals(',') || (p.equals(')') && (len != 4 || strncmp(id, "void", 4))))
			   return 0; // not a function
		   }
		/*
		 * If the last significant character was a ), we need to count
		 * parentheses, because it might be part of a formal parameter
		 * that is a procedure.
		 */
		if (contin > 0)
		{
			int level = 0;

			for (p = skipspace(buf, 1); p; p = skipspace(p + 1, 1))
			level += (p.equals('(') ? 1 : p.equals(')') ? - 1 : 0);
			if (level > 0)
			contin = -1;
		}
		return contin;
	}

	/* Convert a recognized function definition or header to K&R syntax. */
	int convert1(buf, out, header, convert_varargs) byte buf;
		FILE out;
		int header; // Boolean
		int convert_varargs; // Boolean
		{
//C++ TO JAVA CONVERTER TODO TASK: Pointer arithmetic is detected on this variable, so pointers on this variable are left unchanged.
		byte * endfn;
		String p;
		/*
		 * The breaks table contains pointers to the beginning and end
		 * of each argument.
		 */
		String[] breaks;
		int num_breaks = 2; // for testing
		String[] btop;
		String[] bp;
		String[] ap;
		String vararg = 0;

		/* Pre-ANSI implementations don't agree on whether strchr */
		/* is called strchr or index, so we open-code it here. */
		for (endfn = buf; * (endfn++) != '(';)
		  ;
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
	top:
	p = endfn;
//C++ TO JAVA CONVERTER TODO TASK: The memory management function 'malloc' has no equivalent in Java:
//C++ TO JAVA CONVERTER TODO TASK: There is no Java equivalent to 'sizeof':
		breaks = (String)malloc(sizeof(String) * num_breaks * 2);
		if (breaks == DefineConstants.NULL)
		{ // Couldn't allocate break table, give up
			fprintf(stderr, "Unable to allocate break table!\n");
			fputs(buf, out);
			return -1;
		}
		btop = breaks + num_breaks * 2 - 2;
		bp = breaks;
		/* Parse the argument list */
		do
		{
			   int level = 0;
			String lp = DefineConstants.NULL;
			String rp = DefineConstants.NULL;
			String end = DefineConstants.NULL;

			if (bp >= btop)
			{ // Filled up break table.
				/* Allocate a bigger one and start over. */
//C++ TO JAVA CONVERTER TODO TASK: The memory management function 'free' has no equivalent in Java:
				free((String)breaks);
				num_breaks <<= 1;
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
				goto top;
			}
			bp++= p;
			/* Find the end of the argument */
			for (; end.equals(DefineConstants.NULL); p++)
			{
				   switch (p)
				   {
				   case ',':
					if (level == 0)
						end = p;
					break;
				   case '(':
					if (level == 0)
						lp = p;
					level++;
					break;
				   case ')':
					if (--level < 0)
						end = p;
					else
						rp = p;
					break;
				   case '/':
					if (p.charAt(1) == '*')
						p = skipspace(p, 1) - 1;
					break;
				   case '"':
					   p = scanstring(p, 1) - 1;
					   break;
				   default:
					;
				   }
			}
			/* Erase any embedded prototype parameters. */
			if (lp != null && rp != null)
			  writeblanks(lp + 1, rp);
			p--; // back up over terminator
			/* Find the name being declared. */
			/* This is complicated because of procedure and */
			/* array modifiers. */
			for (; ;)
			{
				   p = skipspace(p - 1, -1);
				switch (p)
				{
				   case ']': // skip array dimension(s)
				   case ')': // skip procedure args OR name
				   {
					   int level = 1;
					while (level != 0)
					 switch (*--p)
					 {
					   case ']':
						   case ')':
						   level++;
						   break;
					   case '[':
						   case '(':
						   level--;
						   break;
					   case '/':
						   if (p.compareTo(buf) > 0 && p.charAt(-1) == '*')
						   p = skipspace(p, -1) + 1;
						   break;
					   case '"':
						   p = scanstring(p, -1) + 1;
						   break;
					   default:
						   ;
					 }
				   }
					if (p.equals('(') && skipspace(p + 1, 1).equals('*'))
					{ // We found the name being declared
						while (!((isascii(p) && Character.isLetter(p)) || (p) == '_'))
						  p = skipspace(p, 1) + 1;
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
						goto found;
					}
					break;
				   default:
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
					goto found;
				}
			}
//C++ TO JAVA CONVERTER TODO TASK: There are no gotos or labels in Java:
	found:
	if (p.equals('.') && p.charAt(-1) == '.' && p.charAt(-2) == '.')
	{
				  if (convert_varargs != 0)
				  {
					  bp++= "va_alist";
					vararg = p - 2;
				  }
				else
				{
					  p++;
					if (bp == breaks + 1) // sole argument
					  writeblanks(breaks[0], p);
					else
					  writeblanks(bp[-1] - 1, p);
					bp--;
				}
	}
			else
			{
				   while (((isascii(p) && Character.isLetterOrDigit(p)) || (p) == '_'))
					   p--;
				bp++= p + 1;
			}
			p = end;
		}
		while (p++== ',');
		bp = p;
		/* Make a special check for 'void' arglist */
		if (bp == breaks + 2)
		{
			   p = skipspace(breaks[0], 1);
			if (!strncmp(p, "void", 4))
			{
				   p = skipspace(p + 4, 1);
				if (p.equals(breaks[2] - 1))
				{ // yup, pretend arglist is empty
					   bp = breaks;
					writeblanks(breaks[0], p + 1);
				}
			}
		}
		/* Put out the function name and left parenthesis. */
		p = buf;
		while (!p.equals(endfn))
			putc(p, out), p++;
		/* Put out the declaration. */
		if (header != 0)
		{
			  fputs(");", out);
			for (p = breaks[0]; p; p++)
			  if (p.equals('\r') || p.equals('\n'))
				putc(p, out);
		}
		else
		{
			  for (ap = breaks + 1; ap < bp; ap += 2)
			  {
				  p = ap;
				while (((isascii(p) && Character.isLetterOrDigit(p)) || (p) == '_'))
				  putc(p, out), p++;
				if (ap < bp - 1)
				  fputs(", ", out);
			  }
			fputs(")  ", out);
			/* Put out the argument declarations */
			for (ap = breaks + 2; ap <= bp; ap += 2)
			  ap[-1] = (byte)';';
			if (!vararg.equals(0))
			{
				  vararg = 0;
				fputs(breaks[0], out); // any prior args
				fputs("va_dcl", out); // the final arg
				fputs(bp[0], out);
			}
			else
			  fputs(breaks[0], out);
		}
//C++ TO JAVA CONVERTER TODO TASK: The memory management function 'free' has no equivalent in Java:
		free((String)breaks);
		return 0;
		}

}